<% title = "Moji projekti" %>
<h1 class="mb-4">📁 Moji projekti</h1>

<% if (projekti.length === 0) { %>
  <p>Nemate projekata.</p>
<% } else { %>
  <ul class="list-group">
    <% projekti.forEach(p => { %>
      <li class="list-group-item d-flex justify-content-between align-items-center">
        <strong><%= p.naziv %></strong>
        <a href="/projects/<%= p._id %>" class="btn btn-sm btn-outline-primary">Detalji</a>
      </li>
    <% }) %>
  </ul>
<% } %>
